<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect everything except public folder requests to the public folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
